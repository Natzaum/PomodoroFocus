<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="PomodoroFocus.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:PomodoroFocus"
    Shell.FlyoutBehavior="Flyout">

    <FlyoutItem Title="Pomodoro" Icon="dotnet_bot.png">
        <ShellContent
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
    </FlyoutItem>

    <FlyoutItem Title="Configurações" Icon="dotnet_bot.png">
        <ShellContent
            ContentTemplate="{DataTemplate local:SettingsPage}"
            Route="SettingsPage" />
    </FlyoutItem>

    <FlyoutItem Title="Conquistas" Icon="dotnet_bot.png">
        <ShellContent
            ContentTemplate="{DataTemplate local:AchievementsPage}"
            Route="AchievementsPage" />
    </FlyoutItem>

    <FlyoutItem Title="Tarefas" Icon="dotnet_bot.png">
        <ShellContent
            ContentTemplate="{DataTemplate local:TasksPage}"
            Route="TasksPage" />
    </FlyoutItem>
</Shell>
